/**
 * Workflow Agent
 *
 * Wraps a workflow as an agent interface for composability.
 */

export { createWorkflowAgent } from './agent.js'
export type { WorkflowAgentConfig, WorkflowAgent } from './agent.js'
