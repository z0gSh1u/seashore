{
  "name": "seashore-fullstack-example",
  "version": "0.0.1",
  "private": true,
  "description": "Full-stack chat application with Seashore, Hono, and React",
  "scripts": {
    "install:all": "pnpm install --filter backend && pnpm install --filter frontend",
    "dev:backend": "pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "dev": "concurrently \"pnpm dev:backend\" \"pnpm dev:frontend\"",
    "build:backend": "pnpm --filter backend build",
    "build:frontend": "pnpm --filter frontend build",
    "build": "pnpm build:backend && pnpm build:frontend",
    "start:backend": "pnpm --filter backend start",
    "start:frontend": "pnpm --filter frontend preview",
    "typecheck": "pnpm --filter backend typecheck && pnpm --filter frontend typecheck"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
